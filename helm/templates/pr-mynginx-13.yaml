apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: pr-mynginx-13
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: previews
  source:
    path: helm
    repoURL: https://github.com/nexcollab-bot/{{.REPO}}.git
    targetRevision: HEAD
    helm:
      values: |
        image:
          tag: 
        ingress:
          host: pr-mynginx-13..xip.io
      version: v3
  destination:
    namespace: 
    server: https://kubernetes.default.svc
  syncPolicy:
    automated:
      selfHeal: true
      prune: true
    syncOptions:
    - CreateNamespace=true
